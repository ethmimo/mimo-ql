type Profile {
  id: String
  name: String
  bio: String
}

type Query {
  description: String!
  allProfiles: [Profile]
  profile(id: String!): Profile
}

schema {
  query: Query
  mutation: Mutation
}
